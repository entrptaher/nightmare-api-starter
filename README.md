# nightmare-api-starter
Control Nightmare Instances using API.

It's a unstable WIP, use at your own risk. I'll add things like JSON output, lock file, PM2 controller etc to make sure it works out fine.
